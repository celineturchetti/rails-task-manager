<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<%= link_to "Supprimer #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" }%>
